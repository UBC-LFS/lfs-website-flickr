import {expect} from 'chai';
import Photos from '../src/components/Photos';


// ridiculous test case
describe('Sample test', () => {
    describe('does', () => {
        it('exactly as expected', () => {
           expect('foo').to.not.equal('bar');
        });
    });
});

